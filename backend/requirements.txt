# FastAPI and pydantic were raised together to satisfy the analytics agent
# (app/agents/) — pydantic-ai-slim requires pydantic>=2.12 and
# starlette>=0.46.2, neither of which the previous fastapi 0.104 pin allowed.
fastapi==0.139.2
uvicorn[standard]==0.38.0

# **Pin starlette explicitly, and keep it on 0.x.**
# fastapi 0.139 declares `starlette>=0.46.0` with no upper bound, so a plain
# resolve installs starlette 1.x — a major release fastapi has not adapted
# to. The failure is silent and nasty: `pip check` reports nothing wrong, the
# app imports cleanly, and every included router is quietly dropped, leaving
# only /docs and /health. Do not relax this to a range.
starlette==0.52.1

pydantic==2.13.4
pydantic-settings==2.1.0
sqlalchemy==2.0.23
alembic==1.13.1
psycopg2-binary==2.9.9
requests==2.31.0
crcmod==1.7
python-dotenv==1.0.0
anthropic==0.117.0
httpx==0.28.1

# Unsupervised anomaly detection (app/services/anomaly_detection.py,
# backend/ml/). scikit-learn's IsolationForest is needed at inference time,
# not just training, so it belongs in the runtime image. No TensorFlow: the
# temporal signal is already engineered into the velocity features, so a
# sequence model would add weight without adding information. See
# backend/ml/README.md for why this model was chosen over the alternatives.
numpy==1.26.2
scikit-learn==1.3.2
joblib==1.3.2

# AMQP client for the payment event stream (app/events/). Pinned to 1.3.2
# rather than the newest release: 1.4.x is recent enough that the house
# seven-day cool-down on newly published packages applies, and nothing here
# needs what it added.
pika==1.3.2

# Reserved for work documented in changelog.md "Next Steps" — not yet
# imported anywhere in app/. Kept deliberately so the planned JWT auth and
# Redis caching land without a dependency change.
redis==5.0.1

# JWT session auth (app/core/security.py, app/api/auth.py, app/api/deps.py).
# python-jose already pulls in `cryptography` as an extra, but it's pinned
# explicitly here too: NAMQR signature verification (app/services/
# namqr_processor.py) depends on it directly and a future change to jose's
# extras must not silently drop it.
python-jose[cryptography]==3.3.0
cryptography==49.0.0
# passlib[bcrypt]'s own bcrypt pin resolves to bcrypt>=4.1, which removed
# the __about__ attribute passlib 1.7.4 probes at import and broke its
# 72-byte wrap-bug self-test (ValueError instead of a clean truncation) --
# a known passlib/bcrypt ecosystem incompatibility. Pinned down explicitly
# rather than left to resolve floating, since it crashed app startup.
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-multipart==0.0.6

# Per-IP rate limiting (app/core/limiter.py), applied to /auth/login and the
# device/payment endpoints that have no other throttle.
slowapi==0.1.9

# ---------------------------------------------------------------------------
# Analytics agent (app/agents/) — natural-language analytics with generative
# UI. See docs/analytics-chat.md for the design and AGENT_MASTER_GUIDE for the
# stack rules this follows.
#
# Pydantic AI only, no LangGraph. The guide's own test (§1.0, §2.1) is whether
# a turn needs multi-step orchestration: one agent making one model call plus
# tools per user message is the single-agent path (Part 4.7). This agent reads
# and reports; it has no guardrail loop, no supervisor, and no human-in-the-
# loop gate, because nothing it does writes. Add LangGraph if that changes.
#
# `ag-ui` extra serves the AG-UI protocol straight from FastAPI, which is what
# lets the CopilotKit frontend talk to Python without a Node runtime tier.
# 2.14.1 rather than the current 2.20.0: everything newer is inside the house
# seven-day cool-down.
pydantic-ai-slim[anthropic,ag-ui]==2.14.1

# OpenTelemetry instrumentation for the agent, exported to Langfuse over OTLP
# (app/core/observability.py). Pydantic AI emits spans when the Agent is
# constructed with instrument=True.
logfire==4.38.0
