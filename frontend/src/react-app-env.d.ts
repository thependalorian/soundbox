/// <reference types="react-scripts" />

// This file is normally created by Create React App and was missing here.
// It supplies the module declarations for non-code imports — images, fonts,
// SVG — so `import wordmark from './logo.jpg'` typechecks.
//
// Its absence went unnoticed because nothing imported an asset until the
// brand wordmark did. Any future image, font or SVG import would have hit
// the same error.
